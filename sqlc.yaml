version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/platform/storage/migrations"
    queries: "internal/platform/storage/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        overrides:
          - column: "books.genres"
            go_type: "encoding/json.RawMessage"
        emit_json_tags: true
